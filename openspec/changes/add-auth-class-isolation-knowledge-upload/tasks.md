## 1. Flask 项目与配置基线

- [ ] 1.1 创建 Flask 应用工厂、路由蓝图、SQLAlchemy 和 pytest 骨架；Verify: 运行测试命令时应用可在测试配置下创建且基础测试通过
- [ ] 1.2 实现集中配置并从环境变量读取 `SECRET_KEY`、SQLite 路径、上传目录和文件限制；Verify: 缺少 `SECRET_KEY` 时应用拒绝进入健康状态且错误信息不含敏感值
- [ ] 1.3 配置会话 Cookie、统一 401/403/4xx 响应和敏感日志脱敏；Verify: HTTP 测试确认 Cookie 属性正确，日志中不出现密码、Cookie 或密钥

## 2. SQLite 模型与预置数据

- [ ] 2.1 创建 `users`、`classes`、`class_memberships`、`materials` 和 `knowledge_documents` 模型及建表/迁移逻辑；Verify: 在空 SQLite 数据库执行初始化后表、外键、唯一约束和必要索引存在
- [ ] 2.2 实现基于 scrypt 或 Argon2id 的密码生成与校验服务；Verify: 相同密码生成的哈希不同、正确密码校验成功、用户表不含明文密码
- [ ] 2.3 实现幂等 `flask seed-demo` 命令，创建 A/B 班、A 班教师、A 班学生和 B 班学生；Verify: 连续执行两次后记录不重复且三个账号均可使用环境变量提供的密码登录
- [ ] 2.4 为模型和预置数据增加测试夹具；Verify: 测试可明确取得 A 班与 B 班用户及不同班级成员关系

## 3. 登录、退出与会话保护

- [ ] 3.1 实现登录页面和账号密码登录路由；Verify: 教师与学生正确凭据登录成功，错误密码和不存在账号得到通用失败且不建立会话
- [ ] 3.2 实现当前用户加载、受保护页面装饰器和 API 认证处理；Verify: 未登录页面请求跳转登录页，未登录 API 请求返回 401 且无业务副作用
- [ ] 3.3 实现安全的登录后 `next` 跳转和退出登录；Verify: 站内目标可返回、外部目标被拒绝、退出后原会话不能继续访问受保护资源
- [ ] 3.4 实现服务端教师/学生角色判断且忽略客户端角色字段；Verify: 学生伪造教师角色仍无法通过教师专属检查

## 4. 班级隔离与材料读取

- [ ] 4.1 实现根据 `class_memberships` 判断当前用户可访问班级的服务函数；Verify: A 班用户只得到 A 班 ID，篡改为 B 班时返回 403
- [ ] 4.2 实现本班材料列表查询并只返回 `ready` 材料；Verify: A 班列表不包含 B 班材料，B 班列表不包含 A 班材料
- [ ] 4.3 实现材料详情和文件读取路由并同时按材料 ID 与允许班级过滤；Verify: A 班账号访问 B 班材料详情或文件均返回 403 且响应不泄露材料内容
- [ ] 4.4 增加班级隔离集成测试；Verify: 列表、详情、文件和篡改班级 ID 的跨班用例全部通过

## 5. 教师上传与知识库入库

- [ ] 5.1 实现上传文件存在性、类型、大小和安全文件名校验；Verify: 合法文件通过，空文件、超限文件、不支持类型和路径穿越文件名均被安全处理或拒绝
- [ ] 5.2 实现教师上传页面和上传 API 的会话、角色与班级授权；Verify: 本班教师可进入上传流程，学生直接调用返回 403，教师跨班上传返回 403，拒绝路径均无文件和数据库新增
- [ ] 5.3 实现受控文件保存及 `materials` 与 `knowledge_documents` 的同事务写入；Verify: 成功上传后两表记录关联同一材料和班级，材料状态为 `ready`
- [ ] 5.4 实现数据库失败时的回滚和文件清理；Verify: 注入写入失败后没有材料或知识库记录，上传目录也没有本次孤儿文件
- [ ] 5.5 实现上传成功后的本班材料列表展示；Verify: A 班教师上传后 A 班教师和学生都能查到该记录，B 班学生查不到

## 6. Docker Compose 与健康检查

- [ ] 6.1 实现无需认证的 `GET /health`，检查配置、SQLite `SELECT 1` 和上传目录可写；Verify: 正常返回 200 与 `{"status":"ok"}`，数据库不可用时返回非 2xx 且不泄露路径或堆栈
- [ ] 6.2 创建 Dockerfile 和 Docker Compose，挂载 SQLite 与上传文件持久化卷并注入环境变量；Verify: `docker compose up --build` 后应用可访问且容器状态为 healthy
- [ ] 6.3 验证 Compose 数据持久化与初始化流程；Verify: 执行预置命令并上传材料后重建应用容器，账号、班级、材料、知识库记录和文件仍存在
- [ ] 6.4 编写运行说明和 `.env.example`；Verify: 按文档从空环境完成构建、预置数据、登录、上传、列表查询和健康检查，仓库中不存在真实密钥

## 7. 综合验收与 OpenSpec 校验

- [ ] 7.1 运行认证与密码安全测试；Verify: 覆盖教师/学生登录、错误凭据、未登录跳转、API 401、退出失效和数据库无明文密码
- [ ] 7.2 运行角色与班级安全测试；Verify: 覆盖学生上传 403、角色伪造、教师跨班上传、A 班访问 B 班列表/详情/文件并全部被服务端拒绝
- [ ] 7.3 运行上传入库测试；Verify: 覆盖成功上传后本班列表可查、其他班不可查、知识库记录关联正确和失败回滚无孤儿文件
- [ ] 7.4 在 Docker Compose 环境运行完整测试与健康检查冒烟测试；Verify: 测试全部通过且 `GET /health` 与 Compose healthy 状态一致
- [ ] 7.5 执行 `openspec validate add-auth-class-isolation-knowledge-upload --strict`；Verify: 命令以退出码 0 完成且没有规格格式或场景校验错误
